<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>All Orders</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.7/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    /* Background styling */
    body {
      background: url("https://images.unsplash.com/photo-1504674900247-0877df9cc836?auto=format&fit=crop&w=1920&q=80") no-repeat center center fixed;
      background-size: cover;
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    }

    /* ✅ Apply overlay only to the cart wrapper */
    .cart-container {
      background: rgba(255, 255, 255, 0.85);
      border-radius: 15px;
      padding: 30px;
      box-shadow: 0 8px 20px rgba(0,0,0,0.2);
    }

    .order-card {
      border-radius: 15px;
      overflow: hidden;
      display: flex;
      align-items: center;
      margin-bottom: 20px;
      background: rgba(255, 255, 255, 0.9);
      transition: transform 0.3s ease, box-shadow 0.3s ease;
      height: 200px;
    }
    .order-card:hover {
      transform: translateY(-5px);
      box-shadow: 0 8px 18px rgba(0,0,0,0.25);
    }
    .order-img {
      width: 220px;
      height: 100%;
      object-fit: cover;
    }
    .order-details {
      flex: 1;
      padding: 20px;
    }
    .order-details h5 {
      font-size: 1.6rem;
      font-weight: bold;
      color: #333;
    }
    .order-price {
      font-weight: bold;
      color: #28a745;
      font-size: 1.5rem;
    }
    .delete-btn {
      background: linear-gradient(45deg, #ff4b2b, #ff416c);
      color: white;
      border: none;
      padding: 8px 16px;
      border-radius: 8px;
      font-size: 0.95rem;
      cursor: pointer;
      text-decoration: none;
      transition: 0.3s;
    }
    .delete-btn:hover {
      background: linear-gradient(45deg, #e60023, #cc0022);
      transform: scale(1.05);
    }
  </style>
</head>
<body>

  <%- include('../navandfoot/nav') %>

  <!-- ✅ Only this container has the frosted effect -->
  <div class="container cart-container mt-4">
    <h1 class="mb-4 text-center fw-bold">🛒 Your Cart</h1>

    <% if (cart && cart.items.length > 0) { %>
      <% for (let cartItem of cart.items) { %>
        <div class="order-card">
          <img src="https://via.placeholder.com/220x200" class="order-img" alt="Food Image">
          
          <div class="order-details">
            <h5 class="mb-2"><%= cartItem.itemId.name %></h5>
            <p class="mb-3 text-muted"><%= cartItem.itemId.description %></p>
            <div class="d-flex justify-content-between align-items-center flex-wrap">
              <span class="order-price">₹<%= cartItem.itemId.price %></span>
              <span class="text-muted">Qty: <%= cartItem.quantity %></span>
              <a href="#" class="delete-btn">Delete</a>
            </div>
          </div>
        </div>
      <% } %>
      <a href="/api/cart/order/<%=cart._id%>" class="delete-btn">Buy all items</a>
    <% } else { %>
      <p class="text-center text-white fs-4 fw-semibold">Your cart is empty 🛒</p>
    <% } %>
  </div>

  <!-- ✅ Footer remains transparent -->
  <%- include('../navandfoot/footer') %>
</body>
</html>
