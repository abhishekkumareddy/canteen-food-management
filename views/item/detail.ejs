<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= items.name %> - Order Details</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.7/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      background: url("https://images.unsplash.com/photo-1520962913791-7f6473b60c72") no-repeat center center fixed;
      background-size: cover;
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    }

    /* Dark overlay for better text readability */
    body::before {
      content: "";
      position: fixed;
      top: 0; left: 0; right: 0; bottom: 0;
      background: rgba(0, 0, 0, 0.4);
      z-index: -1;
    }

    /* Glass effect container */
    .order-detail-container {
      max-width: 900px;
      margin: 40px auto;
      background: rgba(255, 255, 255, 0.15);
      border-radius: 16px;
      overflow: hidden;
      backdrop-filter: blur(12px);
      -webkit-backdrop-filter: blur(12px);
      box-shadow: 0 10px 30px rgba(0,0,0,0.3);
      display: flex;
      flex-wrap: wrap;
    }

    .order-img {
      flex: 1 1 50%;
      min-height: 350px;
      object-fit: cover;
      width: 100%;
    }

    .order-content {
      flex: 1 1 50%;
      padding: 30px;
      display: flex;
      flex-direction: column;
      justify-content: center;
    }

    .order-title {
      font-size: 2rem;
      font-weight: bold;
      margin-bottom: 15px;
      color: #fff;
    }

    .order-desc {
      font-size: 1.1rem;
      color: #f5f5f5;
      margin-bottom: 20px;
    }

    .order-price {
      font-size: 1.8rem;
      font-weight: bold;
      color: #00ff9d;
      margin-bottom: 25px;
    }

    .btn {
      padding: 10px 20px;
      font-size: 1rem;
      border-radius: 8px;
      transition: transform 0.2s ease, box-shadow 0.2s ease;
    }

    .btn:hover {
      transform: translateY(-2px);
      box-shadow: 0 5px 15px rgba(0,0,0,0.2);
    }

    .btn-danger {
      background-color: #e63946;
      border: none;
    }

    .btn-success {
      background-color: #06d6a0;
      border: none;
    }

    @media (max-width: 768px) {
      .order-detail-container {
        flex-direction: column;
      }
      .order-img {
        height: 250px;
      }
    }
  </style>
</head>
<body>

  <%- include('../navandfoot/nav') %>

  <div class="order-detail-container">
    <!-- Image -->
    <img src="https://via.placeholder.com/500x350" alt="<%= items.name %>" class="order-img">

    <!-- Content -->
    <div class="order-content">
      <h1 class="order-title"><%= items.name %></h1>
      <p class="order-desc"><%= items.description %></p>
      <p class="order-price">₹<%= items.price %></p>

      <div>
        <%if(curruser && curruser.role == 'admin'){%>
        <a href="/api/item/delete/<%= items._id %>" class="btn btn-danger me-2">🗑 Delete</a>
        <%}else {%>
        <a href="/api/cart/add/<%=items._id%>" class="btn btn-success">🛒 Add to Cart</a>
        <a href="#" class="btn btn-success">buy</a>

        <%}%>
      </div>
    </div>
  </div>

</body>
</html>
